# (Keep your existing openreview-public-service and mongo-cluster-service here)
---
apiVersion: v1
kind: Service
metadata:
  name: openreview-sql-service
spec:
  ports:
  - port: 3306
    targetPort: 3306
  selector:
    app: openreview-sql